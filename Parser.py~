from bs4 import BeautifulSoup

def Parse(html):
  parsedHtml = BeautifulSoup(html)
  allTableRows = parsedHtml.select("table.datadisplaytable tbody tr")
  while allTableRows:
    ParseTableRow(allTableRows)

def ParseTableRow(tableRows)
  dataRow = tableRows.pop()
  headerRow = tableRows.pop()
  course = CreateCourse(dataRow, headerRow)
  section = CreateSection(dataRow, headerRow)

def CreateCourse(dataRow, headerRow)

def CreateSection(dataRow, headerRow)
